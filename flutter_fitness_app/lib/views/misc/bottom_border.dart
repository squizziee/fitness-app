import 'package:flutter/widgets.dart';

Border bottomBorder() {
  return Border(
      bottom: BorderSide(width: 0.5, color: Color.fromRGBO(0, 0, 0, .1)));
}

BorderSide defaultBorderSide() {
  return BorderSide(width: 0.5, color: Color.fromRGBO(0, 0, 0, .1));
}
